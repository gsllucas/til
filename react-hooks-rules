Rules:

- Must not be declared inside a function
- Must not be declared inside a for loop
- Must not be declared inside a conditional

- Should be declared always at the top of the component
- Hooks que can be called inside of a custom hooks
